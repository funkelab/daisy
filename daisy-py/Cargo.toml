[package]
name = "daisy-py"
version = "2.0.0"
edition = "2024"

[lib]
name = "_daisy"
crate-type = ["cdylib"]

[dependencies]
daisy-core = { path = "../daisy-core" }
pyo3 = { version = "0.28", features = ["extension-module"] }
tokio = { version = "1", features = ["full"] }
libc = "0.2"
